---
---

<!-- Development Banner -->
<div class="fixed top-0 left-[-50vw] right-[-50vw] text-white h-[300px] flex items-center justify-center border-y-4 border-gray-700 z-50 shadow-[0_0_100px_rgba(0,0,0,0.8)] overflow-hidden banner-bg">
  <div class="animate-marquee whitespace-nowrap drop-shadow-[0_0_35px_rgba(255,255,255,0.4)]">
    <span class="text-3xl font-bold mx-8 text-glow">☠️ Kehitysversio - Seilataan kartoittamattomilla vesillä</span>
    <span class="text-3xl font-bold mx-8 text-glow">⚔️ Development version - Sailing uncharted waters</span>
    <span class="text-3xl font-bold mx-8 text-glow">⚓ Utvecklingsversion - Vi seglar i okända vatten</span>
    <span class="text-3xl font-bold mx-8 text-glow">☠️ Kehitysversio - Seilataan kartoittamattomilla vesillä</span>
    <span class="text-3xl font-bold mx-8 text-glow">⚔️ Development version - Sailing uncharted waters</span>
    <span class="text-3xl font-bold mx-8 text-glow">⚓ Utvecklingsversion - Vi seglar i okända vatten</span>
  </div>
</div>

<style>
  .banner-bg {
    background-color: #1a1a1a;
    background-image: url("data:image/svg+xml,%3Csvg width='80' height='80' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23404040' fill-opacity='0.6'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
    position: relative;
    box-shadow: inset 0 0 100px rgba(0,0,0,0.5);
    width: 200vw;
  }

  .banner-bg::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(90deg, 
      rgba(26,26,26,0.98) 0%, 
      rgba(26,26,26,0.3) 30%,
      rgba(26,26,26,0.3) 70%,
      rgba(26,26,26,0.98) 100%
    );
    pointer-events: none;
  }

  .text-glow {
    text-shadow: 
      0 0 15px rgba(255,255,255,0.7),
      0 0 25px rgba(255,255,255,0.5),
      0 0 35px rgba(255,255,255,0.3);
  }

  @keyframes marquee {
    0% {
      transform: translateX(100vw);
    }
    100% {
      transform: translateX(-100vw);
    }
  }

  .animate-marquee {
    animation: marquee 30s linear infinite;
    position: relative;
    z-index: 1;
    width: max-content;
    padding-right: 100vw;
  }
</style>

<!-- Spacer for layout purposes -->
<div class="h-0"></div>
